# 05.6: Workspace becomes Projects

**Parent:** 05 (Projects page)

**What to build:** The page has held projects only since 05.2 removed the tasks
list, and scheduled tasks — the other half of what "workspace" meant — are out of
scope (ticket 08, wontfix). The name should follow: /projects, "Projects" in the
nav, and the word gone from the code, not just the label.

**Blocked by:** 05.2 — done

**Status:** done (2026-09-13)

- [x] Route /workspace → /projects; nav reads Projects
- [x] WorkspacePage → ProjectsPage, features/workspace → features/projects,
      useWorkspace → useProjects, workspace.module.css → projects.module.css
- [x] /stats and /calendar routes, pages and nav links removed
- [x] Spec routing table updated
