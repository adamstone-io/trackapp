# 06.7: Prime and Study play the legacy confirmation sound

**Parent:** 06 (Study page core)

**What to build:** The legacy app played a short success tone whenever a prime or
a study interaction was logged (`SoundManager.play("primeLogged")` /
`"studyLogged"`, both `success-tone.mp3` at volume 0.9, fired after the request
came back). The React study page logs silently. Restore the cue.

**Blocked by:** 06.3 (study reveals the answer) — done

**Status:** done (2026-09-13)

- [x] Logging a prime plays the sound
- [x] Logging a study plays it too — on the press that records it, not the
      press that reveals the answer
- [x] It plays on a recorded interaction, not an optimistic one that then
      fails and rolls back (legacy parity: after the request succeeds)
- [x] Same asset and volume as the legacy app
