# 05.2: Remove the tasks list from the workspace page

**Parent:** 05

**What to build:** Owner scope change (2026-09-13): the workspace page shows projects only — no plain-tasks list. This also matches the spec's routing table ("/workspace | Projects + scheduled tasks"; story 62 says projects and *scheduled* tasks, which are ticket 08). Ticket 05's task CRUD UI (TaskSection, task hooks, patch/delete task API fns, task page tests) was removed; tasks are still created implicitly by the timer (`ensureTaskId`) and the backend task endpoints/annotations are unchanged. Consequence: there is currently no UI for editing/archiving/deleting plain tasks — if one is wanted later, it belongs on a future ticket (possibly alongside ticket 08's workspace scheduled-tasks work).

**Status:** done (2026-09-13)

- [x] WorkspacePage renders ProjectSection only
- [x] Dead task UI/hooks/API mutations removed; api/tasks.ts back to ensureTaskId's needs
- [x] Workspace tests cover the projects-only page
